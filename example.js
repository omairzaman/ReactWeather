// var names=['syed','omair', 'zaman'];
//
// names.forEach(function(name) {
//   console.log(name);
// });
//
// names.forEach( (name) => {
//   console.log('arrow',name);
// } );

//names.forEach( (name) => console.log(name) );

var r = (name) => name + "!";

var add= (a,b) => a+b;
console.log(add(1,5));
